<%= include_gon %>
<!-- stock -->
<script src="https://code.highcharts.com/stock/highstock.js"></script>
<script src="https://code.highcharts.com/stock/modules/exporting.js"></script>
<script>
$(function() {
    // $.getJSON('http://www.highcharts.com/samples/data/jsonp.php?filename=aapl-ohlcv.json&callback=?',
    //  function(data) {
      //model-db(active_record)からデータを取得してjsに渡す方法
      //http://qiita.com/s_nakamura/items/5d153f7d9db1b1190296
      // split the data set into ohlc and volume


      var historicals=gon.historical_data

      var ohlc = [],
          volume = [],
          // dataLength = data.length;
          // console.log("length=", dataLength)
          dataLength=historicals.length;
      // alert(data[0][0]+","+data[1][0]+","+data[2][0]+","+data[3][0])
      // var unixtime=data[0][0]/1000
      // alert("original unix=" + unixtime)

      for (i = 0; i < dataLength&&i<historicals.length; i++) {
          var ymd=String(historicals[i]["ymd"])
          var year=ymd.substring(0, 4);
          var month=ymd.substring(4, 6);
          var day=ymd.substring(6, 8);
          var ts = year+"-"+month+"-"+day+" 00:00:00";//"2011-10-14 00:00:00";
          ts=String(ts)
          var unixtime1=Date.parse( ts.replace( /-/g, '/') )

          // alert(ymd)
          // alert(year)
          // alert(month)
          // alert(day)
          // alert(ts)
          // alert( Date.parse( ts.replace( /-/g, '/') ) / 1000 );
          // alert("unixtime="+unixtime1)

          var open=historicals[i]["open"];
          var high=historicals[i]["high"];
          var low=historicals[i]["low"];
          var close=historicals[i]["close"];
          var volume1=historicals[i]["volume"];
          // console.log("ymd=",data[i][0]);
          // console.log("ymd2=", unixtime1);
          ohlc.push([
            unixtime1,
            open,
            high,
            low,
            close
              // data[i][0], // the date : unixtime
              // data[i][1], // open
              // data[i][2], // high
              // data[i][3], // low
              // data[i][4] // close
          ]);

          volume.push([
            unixtime1,
            volume1
              // data[i][0], // the date
              // data[i][5] // the volume
          ])
      }

      // set the allowed units for data grouping
      var groupingUnits = [[
          'week',                         // unit name
          [1]                             // allowed multiples
      ], [
          'month',
          [1, 2, 3, 4, 6]
      ]];


      // create the chart
      $('#container').highcharts('StockChart', {

          rangeSelector: {
              selected: 1
          },
          title: {
              text: historicals[0]["ticker"]//gon.user_name//arrayData[0]//gon.user_name
              // text: array[0]["20151230"]//gon.@pseries.
          },
          yAxis: [{
              title: {
                  text: 'price'
              },
              height: 170,
              lineWidth: 2
          }, {
              title: {
                  text: 'Volume'
              },
              top: 250,
              height: 40,
              offset: 0,
              lineWidth: 2
          }],

          series: [{
              type:'candlestick',
              name: 'Nikkei225',
              data: ohlc,
              dataGrouping: {
                  units: groupingUnits
              }
          }, {
              type: 'column',
              name: 'Volume',
              data: volume,
              yAxis: 1,
              dataGrouping: {
                  units: groupingUnits
              }
          }]
      });
      //console.log("chartsssss=",charts);
      //charts.new()
    // });
});

</script>




<div class="row">
  <div class="col-xs-12 col-sm-12 col-md-9 col-lg-9">
    <!-- main contents field -->

    <div id = "deepblueBack">
      <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
          <div class="row">
            <div class="col-xs-12 col-sm-12 col-md-9 col-lg-9 col-md-offset-1 col-lg-offset-1">
              <br>
              <!-- 最新のデータを取得する -->
              <%= "日経平均株価指数(#{@nikkei225_now2[0].ymd})" %>
              <div id="nikkeiValue">
                <%= number_with_delimiter(@valueNikkei225.to_d.floor(2), :delimiter => ',') %>
              </div>
              <% kigo = "+" %>
              <% if @diffNikkei225.to_f < 0 %>
                <% kigo = "▲" %>
              <% end %>

              <div class="right">

                <%= "前日比: " + kigo + @diffNikkei225.to_f.abs.to_s %>
                <%= "( " + kigo + @returnNikkei225.to_f.abs.to_s + " % )" %>
              </div>
              <br>
            </div>
          </div>
        </div>
      </div>
    </div>

    <br>

    <div class="row">
      <div class="col-xs-12 col-sm-12 col-md-10 col-lg-10 col-md-offset-1 col-lg-offset-1">


        <div id="container" style="width:100%; height:400px;"></div>
        <br>


        騰落率ランキング
        <div class="row">
          <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <% if @up_ranks.length > 0 %>
              <table class="ranking">
                <tr>
                  <th>順位</th>
                  <th>コード</th>
                  <th>上昇率ランキング</th>
                  <th>騰落率(%)</th>
                  <th>価格</th>
                  <th>前日比</th>
                </tr>
                <% @up_ranks.each do |rank_info| %>
                  <% if rank_info.kind_of?(Array) %>
                    <% stock_info = rank_info[1] %>

                    <!-- 10位までに限定：ボタンとかで50位まで表示するオプションをつけてもいいかも -->
                    <% if stock_info["rank"].to_i <= 10 %>
                      <tr>
                        <td><%= stock_info["rank"] %></td>
                        <td><%= stock_info["stock_code"] %></td>
                        <td><%= stock_info["name"] %></td>
                        <%
                          return_value = stock_info["return"]
                          if return_value.length >= 5 #"100.0を最大"
                            return_value = return_value.to_f.round(1)#小数点以下1桁のみ抽出
                          end

                          #floatもしくはintegerの場合のみ％を付与する
                          if ((return_value.to_f).is_a?Float) || ((return_value.to_i).is_a?Integer)
                            return_value = return_value.to_s + "%"
                          end
                        %>
                        <td><%= return_value %></td>
                        <td><%= stock_info["price"].to_i %></td>
                        <td><%= stock_info["vsYesterday"].to_i %></td>
                      </tr>
                    <% end %>
                  <% end %>
                <% end %>

                <tr>
                  <th>順位</th>
                  <th>コード</th>
                  <th>下落率ランキング</th>
                  <th>騰落率(%)</th>
                  <th>価格</th>
                  <th>前日比</th>
                </tr>
                <!-- カラム幅を揃えるため同一テーブルで表示 -->
                <% @down_ranks.each do |rank_info| %>
                  <% if rank_info.kind_of?(Array) %>
                    <% stock_info = rank_info[1] %>

                    <!-- 10位までに限定：ボタンとかで50位まで表示するオプションをつけてもいいかも -->
                    <% if stock_info["rank"].to_i <= 10 %>
                      <tr>
                        <td><%= stock_info["rank"] %></td>
                        <td><%= stock_info["stock_code"] %></td>
                        <td><%= stock_info["name"] %></td>
                        <%
                          return_value = stock_info["return"]
                          if return_value.length >= 5 #"100.0を最大"
                            return_value = return_value.to_f.round(1)#小数点以下1桁のみ抽出
                          end

                          #floatもしくはintegerの場合のみ％を付与する
                          if ((return_value.to_f).is_a?Float) || ((return_value.to_i).is_a?Integer)
                            return_value = return_value.to_s + "%"
                          end
                        %>
                        <td><%= return_value %></td>
                        <td><%= stock_info["price"].to_i %></td>
                        <td><%= stock_info["vsYesterday"].to_i %></td>
                      </tr>
                    <% end %>
                  <% end %>
                <% end %>
              </table>
            <% end %><!-- if @up_ranks.length > 0 -->
          </div>
        </div>

        <br>
        <br>

        <div class="row">

          <br>
          日経225銘柄における騰落率ランキング
          <br>
          <% markets = @rank_others.pluck(:market).uniq %>

          <% markets.each do |market| %>
          <br>
            <% if market == "^N225-3days-return" %>
              <br>
              <h4>3日騰落率ランキング</h4>
              <% days = 3 %>
            <% elsif market == "^N225-5days-return" %>
              <br>
              <h4>5日騰落率ランキング</h4>
              <% days = 5 %>
            <% elsif market == "^N225-7days-return" %>
              <br>
              <h4>7日騰落率ランキング</h4>
              <% days = 7 %>
            <% elsif market == "^N225-10days-return" %>
              <br>
              <h4>10日騰落率ランキング</h4>
              <% days = 10 %>
            <% elsif market == "^N225-30days-return" %>
              <br>
              <h4>30日騰落率ランキング</h4>
              <% days = 30 %>
            <% elsif market == "^N225-3days-change" %>
              <br>
              <h4>3日値動きランキング</h4>
              <% days = 3 %>
            <% elsif market == "^N225-5days-change" %>
              <br>
              <h4>5日値動きランキング</h4>
              <% days = 5 %>
            <% elsif market == "^N225-7days-change" %>
              <br>
              <h4>7日値動きランキング</h4>
              <% days = 7 %>
            <% elsif market == "^N225-10days-change" %>
              <br>
              <h4>10日値動きランキング</h4>
              <% days = 10 %>
            <% elsif market == "^N225-30days-change" %>
              <br>
              <h4>30日値動きランキング</h4>
              <% days = 30 %>
            <% end %>
            <br>
            <% upOrDowns = ["up", "down"] %>
            <% upOrDowns.each do |upordown| %>
              <!-- <% rank_to_display = @rank_others.where(market: market).where(sort: upordown) %> -->
              <% rank_to_display = @rank_others.where(market: market).where(sort: upordown) %>
              <% if upordown == "up" %>
                <%= "上昇率トップ#{rank_to_display.count}" %>
              <% else %>
                <br>
                <%= "下落率トップ#{rank_to_display.count}" %>
              <% end %>
              <div class="row">
                <!-- <%= "ranking = #{@rank_others.where(market: '^N225-10days-change').where(sort: 'down').first.return[0..5]}%" %> -->
                <% rank_to_display.all.each do |rank_Xdays| %>

                  <% reuter_code = rank_Xdays.stock_code.to_s[0..3]+".T" %>

                  <div class="col-xs-12 col-sm-12 col-md-3 col-lg-3">
                    <%= rank_Xdays.rank.to_s + ":" + rank_Xdays.name.to_s + "(" + reuter_code.to_s + ")"%>
                    <%= link_to "http://www.reuters.com/finance/stocks/chart?symbol=#{reuter_code}" do %>
                      <%= image_tag("http://charts.reuters.com/reuters/enhancements/chartapi/chart_api.asp?width=458&height=292&symbol=#{reuter_code}&duration=#{days}&lowers=volume&headertype=none",
                      alt: reuter_code, :width => '192', :height => '96') %>
                      <br>
                      <%= rank_Xdays.name.to_s + "(" + rank_Xdays.return[0..5] + "%)" %>
                    <% end %>
                  </div>
                <% end %>
              </div>
            <% end %>
          <% end %>






          <br>
          日経225銘柄における騰落率ランキング（一週間）

          <br>
          日経225銘柄における騰落率ランキング（１ヶ月）

          <br>
        </div>

      </div>
    </div>

  </div>
  <div class="col-xs-12 col-sm-12 col-md-3 col-lg-3">
    <%= render 'layouts/sidebar' %>
  </div>
</div>
